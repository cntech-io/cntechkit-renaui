import { IconButtonStyleProps } from "./icon-button.props";
declare const iconButtonStyle: (props: IconButtonStyleProps) => {
    root: {
        width: number;
        height: number;
    };
};
export default iconButtonStyle;
